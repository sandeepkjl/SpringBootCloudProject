spring:
  application:
    name: transfer-service

server:
  port: 8089


eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/

limit:
  service:
    name: limit-service